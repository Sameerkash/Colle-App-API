type User {
  id: ID! @id
  password: String!
  email: String! @unique
  name: String!
  posts: [Post!]!
}

type Post {
  id: ID! @id
  title: String!
  body: String!
  author: User! @relation(link: INLINE)
  updatedAt: DateTime!@updatedAt
  createdAt: DateTime!@createdAt
}
